apiVersion: v1
kind: Service
metadata:
  name: vio-vision-backend
  namespace: vio-demo
  labels:
    app: vio-vision-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vio-vision-backend
